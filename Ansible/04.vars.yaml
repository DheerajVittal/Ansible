- name: Play level variables
  hosts: local_servers
  connection: local_servers

  vars:
    course: ansible
    duration: 10HRS
    trainer: sivakumar reddy

  tasks:
  - name: print the course info
    ansible.builtin.debug:
    msg: "course name is :{{ansible}}, duration: {{duration}}, trainer: {{trainer}}"

    